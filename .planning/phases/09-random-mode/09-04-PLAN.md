---
phase: 09-random-mode
plan: 04
type: execute
wave: 3
depends_on:
  - "09-01"
  - "09-02"
  - "09-03"
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "Host sees and can operate the Chaos-Modus slider in the lobby"
    - "Non-host player sees 'Chaos-Modus: Aktiv' when host enables the mode"
    - "At least one chaos event fires during gameplay and shows a German banner"
    - "rotate_piece event visibly rotates the ghost preview"
    - "remove_piece event visibly removes a piece from the grid"
    - "Server test suite passes with zero failures"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Random Mode feature across all 4 event types, the lobby toggle, and the in-game notification banner.

Purpose: Random Mode is a chaos-by-nature feature — automated tests prove individual behaviors but human eyes confirm the UX feels right (banners appear, rotation is visible, piece disappears from grid).

Output: Human approval or bug report.
</objective>

<execution_context>
@/Users/louispoller/.claude/get-shit-done/workflows/execute-plan.md
@/Users/louispoller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-random-mode/09-01-SUMMARY.md
@.planning/phases/09-random-mode/09-02-SUMMARY.md
@.planning/phases/09-random-mode/09-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verification — Random Mode end-to-end (7 scenarios)</name>
  <action>Pause for human to verify all 7 scenarios described in how-to-verify. See details below.</action>
  <what-built>
    Random Mode end-to-end:
    - Lobby toggle slider (host-controlled, non-host read-only)
    - Server: 30% event trigger after each successful piece placement
    - 4 event types: remove_piece, rotate_piece, skip_turn, shuffle_order
    - In-game chaos banner (German, 3-second timeout)
    - Full server test suite (game.test.js + socket.test.js) passing
  </what-built>
  <how-to-verify>
    First confirm tests pass:
    `cd "/Users/louispoller/Library/Mobile Documents/com~apple~CloudDocs/02_Code_Projekte/Uni_Projekte/LogiBlock/server" && node --test src/game.test.js src/socket.test.js`

    Then start the server: `node server/src/server.js` (or however you normally start it)

    **Scenario 1 — Lobby toggle (host):**
    1. Create a room as host
    2. Verify "Chaos-Modus" slider appears in lobby host controls
    3. Slide it to ON — slider snaps to 1

    **Scenario 2 — Lobby toggle (non-host):**
    4. Join the room as a second player (different browser tab/window)
    5. Verify "Chaos-Modus: Aktiv" text appears in the non-host lobby view
    6. Slide back to OFF from the host side — non-host text disappears

    **Scenario 3 — In-game events:**
    7. Start the game with Chaos-Modus ON
    8. Place pieces repeatedly (need ~3-10 placements to trigger 30% event)
    9. Confirm: a German chaos banner appears on screen (e.g. "Chaos! Die Spielerreihenfolge wurde durchgemischt!")
    10. Confirm: banner disappears automatically after ~3 seconds

    **Scenario 4 — rotate_piece event:**
    11. Select a piece in your bank
    12. When a rotate_piece event fires: verify ghost preview rotates 90 degrees

    **Scenario 5 — remove_piece event:**
    13. Place a piece on the grid
    14. When a remove_piece event fires: verify the piece disappears from the grid and returns to the bank

    **Scenario 6 — No events without Chaos-Modus:**
    15. Start a new game with Chaos-Modus OFF (slider at 0)
    16. Place many pieces — confirm no chaos banner ever appears

    **Scenario 7 — No event on win:**
    17. Complete the puzzle (all pieces placed)
    18. Confirm win screen appears without a chaos banner firing simultaneously
  </how-to-verify>
  <verify>
    <automated>cd "/Users/louispoller/Library/Mobile Documents/com~apple~CloudDocs/02_Code_Projekte/Uni_Projekte/LogiBlock/server" && node --test src/game.test.js src/socket.test.js 2>&1 | tail -3</automated>
  </verify>
  <done>Human types "approved" after all 7 scenarios pass.</done>
  <resume-signal>Type "approved" if all scenarios pass, or describe which scenario failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
All server tests pass before human verification begins.
Human confirms: lobby slider works, non-host display works, at least one chaos event fires and shows correct German banner, no event fires on win or without Chaos-Modus enabled.
</verification>

<success_criteria>
- Host slider visible and functional in lobby
- Non-host reads Chaos-Modus state correctly
- At least one in-game chaos event observed with correct German banner text
- Banner auto-dismisses after ~3 seconds
- No chaos event fires on winning move
- No chaos event fires when randomMode is off
- All server tests green
</success_criteria>

<output>
After completion, create `.planning/phases/09-random-mode/09-04-SUMMARY.md`
</output>
