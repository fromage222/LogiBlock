---
phase: 02-game-loop
plan: "05"
type: execute
wave: 4
depends_on:
  - "02-04"
files_modified: []
autonomous: false

requirements:
  - GAME-01
  - GAME-02
  - GAME-03
  - GAME-04
  - GAME-05
  - GAME-07
  - GAME-08
  - PUZZ-03
  - WIN-01
  - WIN-02

must_haves:
  truths:
    - "The game is fully playable end-to-end: pieces can be placed and returned, win screen appears"
    - "Turn indicator is visible to all players at all times"
    - "Non-active players cannot interact with pieces"
    - "Ghost preview shows correct green/red feedback during drag"
    - "Win overlay appears for all players when the puzzle is solved"
  artifacts:
    - path: "server/src/game.js"
      provides: "All game logic functions (placePiece, returnPiece, checkWin, advanceTurn, rotateCells)"
    - path: "server/src/socket.js"
      provides: "game:move handler wired"
    - path: "client/main.js"
      provides: "Complete client game loop"
    - path: "client/index.html"
      provides: "Bank panel, turn banner, player badges, win overlay"
    - path: "client/style.css"
      provides: "All Phase 2 visual styles"
  key_links:
    - from: "client/main.js"
      to: "socket.emit('game:move')"
      via: "drop handler"
      pattern: "game:move"
    - from: "server/src/socket.js"
      to: "io.to(roomCode).emit('game:stateUpdate')"
      via: "after valid place or return"
      pattern: "game:stateUpdate"
    - from: "server/src/socket.js"
      to: "io.to(roomCode).emit('game:win')"
      via: "after winning place"
      pattern: "game:win"
---

<objective>
Human verification of the complete Phase 2 game loop. Confirm all locked decisions from CONTEXT.md are correctly implemented and the game is playable end-to-end.

Purpose: All automated tests and code checks pass — this checkpoint confirms the user experience is correct and the phase is ready to close.
Output: Phase 2 approved or issues documented for gap closure.
</objective>

<execution_context>
@/Users/louisproduktiv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/louisproduktiv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-game-loop/02-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 2 game loop:
    - Server: placePiece, returnPiece, checkWin, advanceTurn, rotateCells (TDD-verified)
    - Server: game:move socket handler with game:error (point-to-point) and game:win (broadcast)
    - Client: Bank panel (right of grid) with per-piece colored mini previews
    - Client: Drag-and-drop from bank to grid with ghost preview (green=valid, red=invalid)
    - Client: Click to select piece + cycle rotation; rotation shown only in drag ghost
    - Client: Click placed cell to return piece to bank
    - Client: Turn banner + player badges with active player highlight
    - Client: Win overlay when puzzle is solved
    - Client: Non-active players fully locked (no drag, no click)
  </what-built>
  <how-to-verify>
    Start server: `cd "/Users/louisproduktiv/Desktop/UNI/Fortgeschrittene Prog/LogiBlock/server" && node src/server.js`
    Open two browser tabs to http://localhost:3000

    **Lobby flow (from Phase 1):**
    1. Tab 1: Enter name "Alice", click Create Room — lobby screen appears with 6-digit code
    2. Tab 2: Enter name "Bob", paste the code, click Join Room — both tabs show 2 players
    3. Alice clicks Start Game — both tabs show game screen

    **Turn indicator (GAME-01, GAME-02):**
    4. Verify: turn banner shows "It's your turn!" in Alice's tab and "It's Alice's turn" in Bob's tab
    5. Verify: Alice's player badge is highlighted (blue glow); Bob's badge is grey

    **Bank panel (locked decision):**
    6. Verify: bank panel appears to the RIGHT of the grid in both tabs
    7. Verify: bank pieces have unique colors with mini-grid shape previews (not just text)
    8. Verify: Bob's bank pieces have no drag cursor (pointer-events disabled, draggable=false)

    **Piece selection and rotation (PUZZ-03, locked decision):**
    9. In Alice's tab: click a bank piece — it should lift (scale transform + shadow) = selected
    10. Click it again — rotation should increment (no visible change in bank preview — that's correct)
    11. Click it once more — rotation increments again
    12. Verify: bank mini-preview does NOT update to show rotation (only ghost on grid changes)

    **Ghost preview (locked decision):**
    13. Drag the selected piece over the grid — ghost cells appear on grid
    14. Hover over valid empty cells — ghost is GREEN
    15. Hover over occupied (anchor) cells — ghost is RED
    16. Press Escape during drag — ghost clears completely (no stale green/red cells)

    **Place move (GAME-03, GAME-05):**
    17. Drop piece on a valid position — piece appears on grid with its color; disappears from bank
    18. Verify: Bob's tab updates immediately (real-time sync — GAME-08)
    19. Verify: turn banner flips — now "It's Bob's turn" in Alice's tab

    **Invalid move (GAME-07):**
    20. In Bob's tab: try to drag a piece (should be impossible — draggable=false)
    21. Alice's turn: try to drop a piece on an anchor cell — piece snaps back; error message appears near grid; only Alice sees the error

    **Return move (GAME-04):**
    22. Alice (or Bob on their turn): place a piece, then click it on the grid
    23. Verify: piece returns to bank; turn does NOT advance (same player still active)
    24. Verify: all players see the bank and grid update

    **Win condition (WIN-01, WIN-02):**
    25. Complete the puzzle by placing all movable pieces in their correct positions
    26. Verify: win overlay appears for ALL players simultaneously
    27. Verify: overlay shows player names and "solved the puzzle!" message
    28. Verify: grid is still visible behind the overlay

    **Disconnect during game (GAME-09 from Phase 1, verify still works):**
    29. With 2+ players in game, close one tab
    30. Verify: remaining player(s) get game:stateUpdate; turn advances correctly
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. Or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human verification as described in the task above.
</verification>

<success_criteria>
All verification steps pass. User types "approved".
</success_criteria>

<output>
After completion, create `.planning/phases/02-game-loop/02-05-SUMMARY.md`
</output>
