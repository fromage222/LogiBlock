---
phase: 03-timer-und-leaderboard
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified: []
autonomous: false
requirements: [TIME-01, TIME-02, TIME-03, TIME-04, TIME-05]

must_haves:
  truths:
    - "the live timer is visible on the game screen and counts up in MM:SS format"
    - "the timer starts when the game starts and stops exactly when the puzzle is solved"
    - "the win card shows the elapsed time as a large, prominent element"
    - "the win card shows player names and a Play Again button"
    - "Play Again returns all players to the start screen"
    - "the start screen leaderboard shows completed games sorted by fastest time"
    - "the leaderboard shows 'No games completed yet' when empty"
    - "the leaderboard updates after each game win without page refresh"
  artifacts:
    - path: "client/index.html"
      provides: "all Phase 3 DOM elements present and accessible"
    - path: "client/main.js"
      provides: "timer + leaderboard logic fully wired"
    - path: "server/src/game.js"
      provides: "leaderboard functions exported and tested"
  key_links:
    - from: "human tester"
      to: "live timer display"
      via: "start a 2-player game, observe #game-timer ticking"
    - from: "human tester"
      to: "win card time hero"
      via: "solve the puzzle, observe large time on win card"
    - from: "human tester"
      to: "leaderboard entry"
      via: "after win, return to start screen, observe leaderboard row"
---

<objective>
Human verification of Phase 3 — confirm all timer and leaderboard features work end-to-end as experienced by real players.

Purpose: Automated tests cover server logic. This checkpoint confirms the visual experience — that the timer ticks visibly, the win card is correctly laid out with the time hero, and the leaderboard populates on the start screen.

Output: Human approval or a list of issues for gap closure.
</objective>

<execution_context>
@/Users/louisproduktiv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/louisproduktiv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-timer-und-leaderboard/03-01-SUMMARY.md
@.planning/phases/03-timer-und-leaderboard/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start server and confirm clean startup</name>
  <files></files>
  <action>
Start the server and confirm it starts without errors:
```bash
cd "/Users/louisproduktiv/Desktop/UNI/Fortgeschrittene Prog/LogiBlock" && node server/src/server.js &
sleep 2
curl -s http://localhost:8000 | head -5
```
Expected: server prints `[LogiBlock] Server running on...` and `[LogiBlock] N puzzle(s) loaded`. The curl should return HTML.

If the server fails to start, do NOT proceed to the checkpoint — fix the error first (syntax error, missing export, etc.).
  </action>
  <verify>
    <automated>cd "/Users/louisproduktiv/Desktop/UNI/Fortgeschrittene Prog/LogiBlock" && node --test server/src/game.test.js server/src/socket.test.js 2>&1 | tail -15</automated>
  </verify>
  <done>Server starts cleanly; all automated tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — timer, win card, and leaderboard</name>
  <what-built>
    Phase 3: Live timer, win card with time hero, Play Again navigation, and session leaderboard.

    - Game screen: a MM:SS timer appears near the turn banner and counts up from 00:00
    - Win card: "Puzzle Solved!" title → large time (MM:SS) → player names → Play Again button
    - Start screen: leaderboard table below the join card (shows "No games completed yet" when empty)
    - Leaderboard: updates automatically after each win, sorted fastest-first
  </what-built>
  <how-to-verify>
    Open two browser tabs to http://localhost:8000 (or the server URL).

    **Step 1 — Start screen leaderboard (empty state):**
    1. Load the start screen.
    2. Scroll below the join card.
    3. Confirm: a leaderboard section is visible with the heading "Leaderboard" and the text "No games completed yet".

    **Step 2 — Live timer:**
    4. In Tab 1: enter a name, click "Create Room", note the room code.
    5. In Tab 2: enter a different name, enter the room code, click "Join Room".
    6. In Tab 1 (host): click "Start Game".
    7. Confirm on BOTH tabs: a timer appears near the turn banner and begins counting up (00:00, 00:01, 00:02...).

    **Step 3 — Win and time display:**
    8. Play through the puzzle until it is solved (place all pieces correctly).
    9. Confirm on BOTH tabs: the win overlay appears with:
       - "Puzzle Solved!" as the title
       - A large time display (MM:SS) as the most visually prominent element below the title
       - Player names listed below the time
       - A "Play Again" button below the names

    **Step 4 — Play Again:**
    10. Click "Play Again" in one tab.
    11. Confirm: the win overlay disappears and the start screen is shown.
    12. Confirm: the leaderboard on the start screen now shows 1 entry with the puzzle name, time, and player names.

    **Step 5 — Leaderboard persists and sorts:**
    13. Start and complete a second game (with any players).
    14. Return to the start screen.
    15. Confirm: the leaderboard shows 2 entries, sorted fastest time first (rank 1 = shortest time).

    **Step 6 — Timer freeze:**
    16. Start another game and let it run for 10+ seconds.
    17. Solve the puzzle.
    18. Confirm: the win overlay timer shows the correct elapsed time (matches roughly how long the game took) and is NOT still counting up.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 steps pass. Describe any issues if not.</resume-signal>
  <action>Human verification — follow the steps in how-to-verify above.</action>
  <verify>
    <automated>MISSING — this is a human checkpoint; verification is performed manually per how-to-verify steps</automated>
  </verify>
  <done>Human types "approved" confirming all 6 verification steps pass</done>
</task>

</tasks>

<verification>
Automated tests pass (confirmed in Task 1). Human verification confirms all TIME-01 through TIME-05 requirements are satisfied visually.
</verification>

<success_criteria>
- Live timer visible and ticking during gameplay (TIME-01, TIME-02)
- Win card shows large prominent time display in MM:SS (TIME-03)
- Start screen shows leaderboard that populates after wins (TIME-04)
- Leaderboard is empty on first load and resets after server restart (TIME-05)
- Play Again navigation works for all players
</success_criteria>

<output>
After human approval, create `.planning/phases/03-timer-und-leaderboard/03-03-SUMMARY.md` with:
- Verification result (approved/issues)
- Any issues found and whether they were fixed inline or deferred to gap closure
- Final status of Phase 3
</output>
