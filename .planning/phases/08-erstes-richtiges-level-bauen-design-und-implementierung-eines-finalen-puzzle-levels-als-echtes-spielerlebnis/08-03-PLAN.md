---
phase: 08-erstes-richtiges-level-bauen
plan: "03"
type: execute
wave: 3
depends_on:
  - "08-01"
  - "08-02"
files_modified: []
autonomous: false
requirements:
  - LVL-06

must_haves:
  truths:
    - "Host sees 'Level 1 — Einfach' as the only option in the puzzle dropdown"
    - "Non-host sees 'Ausgewähltes Puzzle: Level 1 — Einfach' in the lobby"
    - "3 anchor pieces are visible and locked on the grid at game start"
    - "7 movable pieces appear in the bank panel at game start"
    - "The puzzle is playable end-to-end (pieces can be placed, win fires)"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Level 1 experience: lobby puzzle selection and in-game anchor/movable piece layout.

Purpose: No automated tests cover visual rendering or live multiplayer flows. This checkpoint confirms the entire Phase 8 feature works as specified before phase completion is recorded.

Output: Human approval (or defect report to be fixed before close).
</objective>

<execution_context>
@/Users/louispoller/.claude/get-shit-done/workflows/execute-plan.md
@/Users/louispoller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-erstes-richtiges-level-bauen-design-und-implementierung-eines-finalen-puzzle-levels-als-echtes-spielerlebnis/08-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Level 1 lobby and gameplay end-to-end</name>
  <action>Human verifies the complete Level 1 experience across 5 scenarios listed below.</action>
  <what-built>
    - puzzles/level_01.json — Level 1 puzzle with 3 anchors (A, B, C) and 7 movable pieces (P01–P07), difficulty "easy"
    - server/src/game.js — getPuzzleListForClient filters to difficulty-tagged puzzles only; createLobby defaults to level_01; getPublicState exposes selectedPuzzleDifficulty; validatePuzzleSchema accepts optional difficulty string
    - client/main.js — DIFFICULTY_LABELS constant; puzzle:list dropdown shows "Name — Einfach"; non-host lobby shows "Ausgewähltes Puzzle: Level 1 — Einfach"
  </what-built>
  <how-to-verify>
Start the server:
```
npm run dev
```
(or `node server/src/server.js` if watch mode is not desired)

**Scenario 1 — Lobby puzzle dropdown (host view):**
1. Open http://localhost:3000 in a browser
2. Enter a name and click "Create Room"
3. Verify: the puzzle dropdown shows ONLY "Level 1 — Einfach" (no puzzle_01, no puzzle_02, no "Corner Cut")

**Scenario 2 — Non-host puzzle display:**
1. Open a second browser tab (or incognito) at http://localhost:3000
2. Enter a different name and join the same room code
3. Verify: the second player sees "Ausgewähltes Puzzle: Level 1 — Einfach" (NOT "Selected puzzle: Level 1")

**Scenario 3 — Game start: anchors pre-placed:**
1. With 2 players in the lobby, the host clicks "Start Game"
2. Verify: 3 pieces are already visible on the grid, cannot be clicked or moved (they are anchors)
3. Verify: the 3 anchor cells display the correct piece colors and are NOT in the bank panel

**Scenario 4 — Game start: 7 movable pieces in bank:**
1. After starting the game (Scenario 3)
2. Verify: the bank panel shows exactly 7 pieces (P01–P07)
3. Verify: selecting a bank piece and hovering the grid shows the ghost preview

**Scenario 5 — Win condition:**
1. Place all 7 movable pieces on the grid in valid positions
2. Verify: the win screen / win message appears once the final piece is placed correctly
3. (If testing full completion is time-consuming, verifying Scenarios 1–4 is the minimum bar)
  </how-to-verify>
  <verify>
    <automated>MISSING — no automated test covers visual rendering; manual verification required per scenarios above</automated>
  </verify>
  <done>Human types "approved" after passing all scenarios, or files a defect</done>
  <resume-signal>Type "approved" if all scenarios pass, or describe any failing scenario</resume-signal>
</task>

</tasks>

<verification>
Human approval via checkpoint above.
</verification>

<success_criteria>
- All 5 scenarios verified manually
- No regressions in existing lobby or game functionality
- Level 1 is playable from lobby selection through to win condition
</success_criteria>

<output>
After completion, create `.planning/phases/08-erstes-richtiges-level-bauen-design-und-implementierung-eines-finalen-puzzle-levels-als-echtes-spielerlebnis/08-03-SUMMARY.md`
</output>
